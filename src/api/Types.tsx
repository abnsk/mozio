export type CityInfo = {
    name: string,
    latd: number,
    long: number
}

export type CitySelectData = {
    value: string,
    label: string
}

export type DistanceResultObject = {
    origin: string,
    destination: string,
    distance: number
}